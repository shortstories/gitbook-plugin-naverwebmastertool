# gitbook-plugin-verifications

## book.json

``` json
{
    "plugins": ["verifications"],
    "pluginsConfig": {
        "verifications": [
            {
                "name": "meta tag name",
                "content": "meta tag content",
            }
        ]
    }
}
```
